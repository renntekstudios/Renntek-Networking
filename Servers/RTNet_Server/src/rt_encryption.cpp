#include "rt_encryption.h"

using namespace RTNet;

rt_byte* RTEncryption::Encrypt(rt_byte* input, int length)
{
    return nullptr;   
}

rt_byte* RTEncryption::Encrypt(rt_byte* input, int length, int* outLength)
{
    outLength = 0;
    return nullptr;   
}